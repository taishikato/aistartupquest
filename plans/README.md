# Implementation Plans

Generated by the improve skill on 2026-07-06 at commit `ba0778c`; extended and reconciled on 2026-07-11 at commit `16f120d`.
Execute in the order below unless dependencies say otherwise.
Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Upgrade Next.js past the current security advisories | P1 | S | - | TODO |
| 002 | Add anti-abuse protection to company submission requests | P1 | M | - | TODO |
| 003 | Verification baseline: CI, server-action tests, zero lint warnings | P1 | M | - | TODO |
| 014 | Make the cursor.com event fetch resilient to malformed and drifted upstream data | P1 | S | - | TODO |
| 004 | Remove dead code and misplaced dependencies | P2 | S | 001 (soft) | TODO |
| 005 | Small correctness and observability fixes (bundle) | P2 | S | - | TODO |
| 015 | Extract and unit-test the top page's upcoming-events logic; slim its client payload | P2 | S | - | TODO |
| 016 | Stop rebuilding every top-page event marker on each selection and keystroke | P2 | M | 015 (soft), 003 (soft) | TODO |
| 006 | Stop rebuilding every map marker on each selection change (map-shell) | P2 | M | 003 | TODO |
| 007 | Decompose the 1383-line map-shell.tsx | P3 | L | 003, 006 | TODO |
| 008 | Refresh README and fix the stale genType command (amended 2026-07-11) | P2 | S | - | TODO |
| 009 | Design spike: automated event sync and the second guild (amended 2026-07-11) | P3 | M | - | TODO |
| 010 | Design spike: submission lifecycle (moderation, edit, withdrawal) | P3 | M | - | TODO |
| 011 | Design spike: resolve the world-map experiments (largely superseded; amended 2026-07-11) | P3 | S | - | TODO |
| 012 | Events-first top page | - | - | - | DONE (implemented by maintainer, `0e7beeb`) |
| 013 | Event company tag design | - | - | - | DONE (implemented by maintainer, `40d8fab`, `16f120d`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Reconciliation log

- 2026-07-10 at `ba0778c`: all 11 plans remained TODO; no source drift from the planning commit; plan 001 refreshed for newer Next.js advisories (minimum safe version `16.2.6`).
- 2026-07-11 at `16f120d`: delta audit of everything changed since `ba0778c` (the events-first top page rewrite, the cursor.com fetch pipeline, quest markers, company tags).
  - Verification: `pnpm typecheck`, `pnpm test` (18 tests), and `pnpm lint` (now zero warnings) all pass; `pnpm audit --prod` still reports 54 production vulnerabilities (15 high) driven by Next.js `16.1.7` — plan 001 remains the top P1.
  - Plans 012 and 013 were authored and implemented by the maintainer outside this index; recorded as DONE.
  - Plan 011 is largely superseded: the maintainer deleted `world-globe-select.tsx`, `world-map-select.tsx`, `events-world-map.tsx`, and `space-backdrop.tsx`; only the `/map-libre` decision remains (premise update in the plan; effort now S).
  - Plans 008 and 009 received premise updates for route changes (`/` is the events map; `/events` permanent-redirects) and for the new fetch pipeline.
  - New plans 014-016 cover the delta findings.
  - During the audit the maintainer committed `0adafe4` (removes the compact city-nav from `home-events-map.tsx`); plans 015/016 document it as expected drift from their `16f120d` line references.
- Executable immediately: 001, 002, 003, 005, 008, 009, 010, 011, 014, 015.

## Dependency notes

- 004 is soft-ordered after 001 so `pnpm-lock.yaml` churn happens in two clean commits instead of conflicting ones.
- 006 and 007 require 003 because no automated tests cover the map components; the CI gate (lint zero-warnings, typecheck, test) is the only regression net for those refactors.
- 007 requires 006 to land first so the diffing logic moves into the new `useMapMarkers` hook wholesale; running them in the other order voids 006's line references.
- 015 and 016 both edit `components/home-events-map.tsx`; run 015 first (016's drift check anticipates 015's extraction). 016 also soft-depends on 003 for the same no-test-net reason as 006.
- 016 deliberately does NOT extract a marker module shared with `map-libre-world-select.tsx` or `map-shell.tsx`; that consolidation waits on 011's verdict and 006/007 landing.
- 002 and 010 touch the same submission surface: implement 002 (security fix) first; 010 is a design doc and should be written against post-002 code if 002 has landed.
- 009 and 010 interact: guild imports publish immediately (trusted source); any moderation design from 010 must state that explicitly.
- 014 hardens the script that 009's automation design will schedule; 014 first is preferred but not required.

## Audit coverage note

- 2026-07-06 standard audit at `ba0778c`: correctness and security swept very thoroughly (all of `app/`, `lib/`, `components/`, migrations); performance, debt, dependencies, tests, DX, docs, direction hotspot-weighted.
- 2026-07-11 standard delta audit at `16f120d`: all nine categories over the files changed since `ba0778c` only (`components/home-events-map.tsx`, `lib/cursor-events-fetch.ts`, `scripts/fetch-cursor-events.ts`, `app/events/page.tsx`, `app/page.tsx`, event data files, tests); correctness/security very thorough, rest medium.
- Not audited in either pass: `public/` assets, `docs/superpowers/` (vendored), generated `types/supabase.ts` internals, and remote Supabase state beyond what migrations show.
- Non-interactive run note (2026-07-11): no maintainer was available to select findings, so plans were written for the top findings by leverage per the skill's default; the full finding-to-plan mapping is in each plan's "Why this matters".

## Findings considered and rejected

Recorded so nobody re-audits them:

- **Atlas style retry storm** (events-world-map): claimed infinite retry on failed style loads — actually the catch falls back to the art style and pins the user choice; no loop. Not a finding. (Component since deleted.)
- **Marker/city index mismatch** (events-world-map `repositionMarkers`): `upcomingCities` is `useMemo(..., [])` — computed once, immutable; the index race cannot occur. (Component since deleted.)
- **Unmounted setState in world-globe-select meetup counts**: a `disposed` flag is checked before `setMeetupCounts`; already correct. (Component since deleted.)
- **Marker click-listener memory leak** (map-shell): removed DOM elements are garbage-collected with their listeners; no explicit removeEventListener needed.
- **Server Action CSRF**: Next.js Server Actions ship origin-checked CSRF protection; nothing to add.
- **SRI hashes on the Turnstile script**: the Cloudflare script updates frequently; SRI would break the widget. Impractical.
- **"Company requests broken for 4 of 6 cities" (RLS city list)**: the server action uses the service-role client which bypasses RLS, so inserts succeed for all six cities. The stale anon policy is folded into plan 002.
- **`world-map-select.tsx` is dead code**: was the live WebGL-failure fallback at `ba0778c`; the maintainer deleted it with the top-page rewrite. Moot.
- **`new AbortController().signal` discarded** (former events-world-map:436): real but harmless; noted as out-of-scope in plan 005. The new `home-events-map.tsx` wires the signal correctly.
- **SVG/canvas marker redesign**: high risk to signature visuals for unproven perf gain; revisit only if marker counts grow past ~500 (see plans 006/016 maintenance notes).
- **`x-forwarded-for` spoofing of the meetup rate limit**: platform-dependent header handling; deployment concern, not a code fix.
- **2026-07-11: "`validateCursorEvent` is referenced only by tests"** (subagent claim): mis-attributed — `scripts/import-cursor-events.ts:36` applies it on the Supabase import path. The real, narrower gap (fetch parser emits unvalidated URLs) is fixed by plan 014.
- **2026-07-11: hydration risk in `getUpcomingCities` per-call `today`**: a server render and client hydration straddling a UTC midnight can still mismatch, but the maintainer already chose this tradeoff in `b455cd1` (per-call compute); the residual window is seconds per day and self-heals on interaction. Not a finding; plan 015's tests pin the boundary semantics.
- **2026-07-11: city-sign markers effect rebuilds on `mapReady` change** (`home-events-map.tsx:416-436`): fires once per map instance for a static six-entry list; no churn in practice. Not worth changing.
- **2026-07-11: `GuildBoardHeader`/`GuildBoardList` duplicated between desktop aside and mobile sheet**: intentional responsive composition of shared components, not duplication; the components ARE the extraction. Not a finding.
- **2026-07-11: local event/city types re-declared in `home-events-map.tsx`** (vs `lib/cursor-events.ts` shapes): the shapes genuinely differ (`company`, coordinates); plan 015 moves them to `lib/cursor-community-events.ts` as the single source for the top-page dataset. Folded into 015, not a separate finding.
